{
  "name": "glottai-react-site",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^15.6.1",
    "react-dom": "^16.0.1"
  },
  "devDependencies": {
    "eslint": "^4.1.1",
    "eslint-plugin-react": "^7.1.0",
    "eslint-config-google": "^0.8.0",
    "react-scripts": "^1.0.11"
  },
  "scripts": {
    "prestart": "sass --update .",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom"
  }
}
